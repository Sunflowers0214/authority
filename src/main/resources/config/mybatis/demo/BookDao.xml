<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flysnow.demo.dao.BookDao">

	<insert id="insert" parameterType="Book">
		insert into d_book(book_id,name,number) values(#{bookId},#{name},#{number})
	</insert>

    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultType="Book" parameterType="String">
        SELECT book_id, name, number
        FROM d_book
        WHERE book_id = #{bookId}
    </select>

    <select id="queryList" resultType="Book">
		SELECT book_id, name, number
		FROM d_book
		ORDER BY book_id
	</select>

    <update id="reduceNumber">
		UPDATE d_book
		SET number = number - 1
		WHERE book_id = #{bookId}
		AND number > 0
	</update>

</mapper>